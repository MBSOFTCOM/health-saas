<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.ppd.dal.mysql.screenrepeatperson.ScreenRepeatPersonMapper">


    <select id="isNullByIdNumYear" resultType="java.lang.Long">
        SELECT id
        FROM tb_screen_repeat_person
        WHERE deleted = 0
          AND id_num = #{idNum}
          AND year = #{year}
          AND screen_type = #{screenType}
    </select>


    <select id="getIsRemainRepeatPerson" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM tb_screen_repeat_person
        WHERE deleted = 0
    </select>


    <select id="isExist" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM tb_screen_person
        WHERE deleted = 0
          AND id_num = #{idNum}
          AND year = #{year}
          AND screen_type = #{screenType}
    </select>

</mapper>